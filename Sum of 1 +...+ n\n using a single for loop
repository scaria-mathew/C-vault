#include<stdio.h>
int main()
{
	float i,n,f,s;
	printf("Enter the value for n: ");
	scanf("%f", &n);
	
	f = 1;
	s = 0;
	
	for(i=1;i<=n;i++)
	{
	    f = f*i;
	    s = s + i/f; 
	}
	printf("%f", s);
	return 0;
}
