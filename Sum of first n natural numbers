#include<stdio.h>
int main()
{
    int n, i=1, s=0;
    printf("Enter n: ");
    scanf("%d", &n);
    while(i<=n)
    {
        s=s+i;
        i++;
    }
    printf("The sum: %d", s);
    return 0;
}
